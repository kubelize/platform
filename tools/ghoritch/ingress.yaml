apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress-1234
  namespace: growth-vat
spec:
  ingressClassName: nginx
  rules:
  - host: ghoritch.a01.kubelize.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: warpfuel-pipe
            port:
              number: 8080