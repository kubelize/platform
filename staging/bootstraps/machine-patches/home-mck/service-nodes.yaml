machine:
    network:
        hostname: <hostname>
machine:
  kubelet:
    extraArgs:
      rotate-server-certificates: "true"
cluster:
  controllerManager:
    extraArgs:
      bind-address: 0.0.0.0
  scheduler:
    extraArgs:
      bind-address: 0.0.0.0